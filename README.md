# core-dns-editor

```sh
usage: coredns_editor.py [-h] --ip IP --hostname HOSTNAME [-i] [-o OUTPUT] input

positional arguments:
  input

options:
  -h, --help            show this help message and exit
  --ip IP
  --hostname HOSTNAME
  -i, --in-place
  -o OUTPUT, --output OUTPUT
```